import columns from "@/types/v2/datagrid/columns/columns";
import { date } from "./date";
import { vehicleLicensePlate } from "./vehicleLicensePlate";

export const fields: columns[] = [
    date,
    vehicleLicensePlate,
];
