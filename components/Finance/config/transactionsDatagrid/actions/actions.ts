import type actions from "@/types/v2/datagrid/actions/actions";

export const actions: actions = {
    config: {
        field: 'actions',
        headerName: 'Acciones',
        width: 150,
    },
    options: [],
}
